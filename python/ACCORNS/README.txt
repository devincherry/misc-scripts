This repo should be turned into a SVN repo on the target installation machine. Then, you should checkout a copy for general use, and the script(s) will commit changes to collected configs whenever the scripts are executed. This allows for version tracking of the collected configs.

